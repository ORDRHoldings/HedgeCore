# NEXUS Current State

> Auto-generated by NEXUS

## Sessions: 15 total, 15 completed
## Active Patterns: 10
## Last Updated: 2026-03-29T00:00:00.000000+00:00

## Current Sprint
**Sprint 5 — Scale & Performance** — COMPLETE (2026-03-29)
- Items: 5 DONE / 5 total
- All enterprise readiness sprints (Sprint 1-5) now complete

## Recent Work (2026-04-04) — Production Auth + Dashboard Fixes
- ROOT CAUSE: Schema drift — ORM model columns missing from production DB. `users.ui_preferences` and 5 `companies` columns (sso_provider, sso_domain, stripe_customer_id, stripe_subscription_id, plan_tier) absent. SQLAlchemy SELECT * → ProgrammingError → swallowed as 401 → dashboard black screen for all users.
- FIXES: ALTER TABLE added to `_ensure_tables()` for all missing columns; `ui_preferences` marked `deferred()` in ORM; `/auth/me` exception handler now returns 500 for non-auth errors; Alembic migrations 0012 + 0013.
- DASHBOARD: Fixed `toFixed()` crash on null `bid/mid/ask` in FX rate cards; made `fmtUsd()` null-safe; guarded `hedgeCoverage` and `hedge_ratio`.
- BROWSER CONFIRMED: Login → /dashboard renders. "Good morning, Demo" greeting visible. KPI strip, TradingView chart, nav all functional. Zero JS errors.
- Validation: 4801 passed, 0 failed, 158 skipped (unchanged)
- Commits: 006b593 → ba269ba → 10ce559 → 3e0e7ab → 14e7ab8 → d1063b6 → 4a6f8ae

## Previous Work (2026-03-29) — Sprint 5: Scale & Performance COMPLETE
- k6 load test script: `docs/performance/k6-load-test.js` (100 VU scenario); baseline doc pending full staging run
- Redis market data cache: `backend/app/core/redis_client.py` — fail-open singleton, 60s TTL, hit/miss counters on /system/health
- Connection pool tuning: DB_POOL_SIZE=20, DB_MAX_OVERFLOW=10, DB_POOL_TIMEOUT=30, DB_POOL_PRE_PING=True in Settings; `create_engine_from_url()` in db.py
- Webhook support: POST/GET/DELETE /v1/webhooks; WebhookEndpoint + WebhookDeliveryLog models; HMAC-SHA256 signed; 5-attempt exponential backoff (1m/5m/15m/60m); WORM audit event on delivery; 4 events: position.created, calculation.completed, proposal.approved, proposal.rejected
- Horizontal scaling prep: SYSTEM_BOUNDARIES.md updated with multi-instance topology; `docs/architecture/horizontal-scaling-contract.md` created; rate limit Redis wiring confirmed
- Validation: 4801 passed, 0 failed, 158 skipped; 27 files changed, 2196 insertions

## Previous Work (2026-03-28) — Sprint 4: Compliance Pipeline COMPLETE
- SOC2 evidence table: `compliance_evidence` WORM table + nightly export job at 02:00 UTC
- GDPR anonymisation job + data rights: GET /v1/user/data-export, DELETE /v1/user/account
- PostgreSQL RLS: `backend/app/core/rls.py` + migration `k1a2b3c4d5e6`
- Vendor security registry: `docs/compliance/vendor-registry.md` — 10 vendors with DPA status
- Validation: 4767 passed, 0 failed, 158 skipped

## Open Risks
- HIGH: Secret rotation pending (S-01) — WORKOS_API_KEY, WORKOS_CLIENT_ID, STRIPE_SECRET_KEY_TEST, STRIPE_WEBHOOK_SECRET not yet added to Render env vars
- HIGH: IBKR market data (R-002) — awaiting TWS
- HIGH: Secrets in git history (R-001) — scrub script ready, not yet run
- HIGH: k6 load test baseline incomplete — full staging run (100 VUs, 5 min) not yet executed
- MEDIUM: Cold starts (RISK-INF-01) — keepalive cron added, Blueprint Sync on Render still pending
- MEDIUM: Render Blueprint Sync — render.yaml changes (Redis, starter plans) not yet applied via dashboard

## Validation
- Backend tests: 4801 passed, 0 failed, 158 skipped (Sprint 5 final)
- Coverage: 75%+ (gate: 60%)
- CI: gitleaks + Dependabot + Trivy + coverage gate all active

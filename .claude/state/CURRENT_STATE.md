# NEXUS Current State

> Auto-generated by Historian agent

## Sessions: 31 total, 31 completed
## Active Patterns: 11
## Last Updated: 2026-05-25

## Recent Work (2026-05-25, later10) — PG marker-suite drain to 0 fails (RISK-CI-PG-01 hard-gate-ready)

Continuation of (later9). Drained the `requires_postgres` marker suite from 83 fails → 0 against probe2 PG (`hedge_test@localhost:5499`). End result: `pytest tests/ -m requires_postgres` → **154 passed / 5520 deselected / 0 failed in 164s**. RISK-CI-PG-01 advisory job can now be flipped to a hard gate once N consecutive green runs land in real CI (currently blocked on org billing).

**Five fixes**:
1. `migrations/env.py` — `transaction_per_migration=True` so a single migration crash doesn't roll back the entire prior chain.
2. `migrations/versions/0028_tca_permissions.py`, `0030_counterparty_permissions.py`, `0032_regulatory_permissions.py` — replaced `INSERT INTO permissions (id, name, ...)` (UUID-into-SERIAL + nonexistent column) with canonical `(codename, module, action, description, created_at) ... ON CONFLICT (codename)`. Role grants join on `p.codename`. Same pattern as t1's prior-arc fix.
3. `app/main.py::_ensure_tables` — added `auth_audit_logs` (3 PG ENUMs via `DO $$ ... EXCEPTION WHEN duplicate_object`, table, 6 indexes). The chain's late migration `3450c02f9c01` never ran when alembic-in-isolation crashed earlier.
4. `app/main.py::_ensure_tables` — `ALTER TABLE users ADD COLUMN IF NOT EXISTS created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()` + `token_version INTEGER NOT NULL DEFAULT 1` + restore the stripped server default. Init migration omits both columns; ORM model requires them.
5. `tests/test_auth_cookies.py::_fresh_settings_class` — snapshot+restore `JWT_SECRET` around `importlib.reload(cfg_module)`. The 4 residual end-of-(later9) fails were `ValidationError`: `test_e2e_policy_lifecycle.py:30` sets a 25-char redacted marker at module-import time, failing the ≥32 validator on reload.

**Verification**:
- PG marker subset (probe2): 154 / 5520 / **0 failed** in 164s.
- SQLite smoke: 138 passed / 4 skipped (PG-only) — no regression.
- `alembic heads`: `0036_force_rls_tenant_context (head)`.

**Gotchas**:
- Bash `cmd 2>&1 | tail -N; EXIT=$?` captures tail's exit, not cmd's.
- `pytest.ini addopts = -x -q --tb=short` truncates fail counts; `-o addopts=` overrides for full drain triage.
- Module-level `os.environ[...] = ...` is contamination; refactor to `monkeypatch.setenv` in a fixture or wrap consumer.
- `permissions` canonical: `id SERIAL`, `codename UNIQUE` (not `name`). Pattern-hunt next time any migration touches the table.

**Repo state**: working tree dirty pending the drain bundle commit. Files changed: `app/main.py`, `migrations/env.py`, `migrations/versions/{0028,0030,0032}*.py`, `tests/test_auth_cookies.py`. SQLite baseline 5514 / 160 / 0 unchanged.

## Recent Work (2026-05-25, later9) — Stub migration `gg1a2b3c4d5e7` + chain content fixes (RISK-CI-PG-02 option-a)

Implemented option (a) from the (later8) arc closure: a single mid-chain stub migration that pre-creates ORM-only tables for alembic-in-isolation, plus three migration content fixes. Net effect: chain reach advanced from "crashes at `h1a2b3c4d5e6`" (chain depth ~5) → "crashes at `0028_tca_permissions`" (chain depth ~40). **~35 migrations of additional alembic-isolation headroom**.

- **New migration**: `gg1a2b3c4d5e7_stub_companies_for_alembic_isolation.py` (between `g1a2b3c4d5e6` and `h1a2b3c4d5e6`). Pre-creates 5 ORM-only tables (`companies`, `legal_entities`, `permissions`, `roles`, `role_permissions`) via `CREATE TABLE IF NOT EXISTS` — schemas match `_ensure_tables()` verbatim. No-op against production (tables already exist via Base.metadata.create_all).
- **Chain rewire**: `h1a2b3c4d5e6.down_revision` → `gg1a2b3c4d5e7`.
- **Idempotency**: `0013_add_sso_billing_to_companies` and `0017_legal_entities` made idempotent against pre-existing columns/tables (raw-SQL `DO $$ IF NOT EXISTS` blocks; required because h1 and 0013 historically duplicate the same 5 columns on `companies`).
- **Structural bug fix**: `t1a2b3c4d5e6_add_ir_debt_permissions` — removed `id` from INSERT into `permissions`. Migration was inserting `uuid4()` into `SERIAL INTEGER` column (`permissions.id` is integer per ORM and `_ensure_tables`). Production tolerated via `run_alembic_upgrade()` swallow + `_seed_permissions` populating data idempotently.
- **Env fix**: `migrations/env.py` widens `alembic_version.version_num` from default `VARCHAR(32)` to `VARCHAR(255)` before running migrations (`0013_add_sso_billing_to_companies` is 33 chars; production tolerated via run_alembic_upgrade swallow + chain healing).

**End-to-end verification on probe2 PG** (port 5499): dropped/recreated `public` schema, ran `alembic upgrade head` against empty database. Chain advanced ~35 migrations past prior crash; new crash at `0028_tca_permissions` line 29 — `column "name" of relation "permissions" does not exist`. Three migrations (`0028_tca_permissions`, `0030_counterparty_permissions`, `0032_regulatory_permissions`) reference a `name` column that doesn't exist (canonical is `codename`) AND insert UUIDs into `permissions.id`. **Migration content bugs** identical to t1's pattern, not chain-structural.

**Diminishing-returns inflection — STOPPING**. From here, each remaining crash requires per-migration content fixes (wrong column names, UUID/SERIAL mismatches, more missing FK targets). The stub amortized over ~35 migrations; individual content fixes do not amortize. The durable solution remains `17a1cc0` workflow refactor — production is already protected; advisory CI gate's marginal value does not justify continued per-migration work.

**Verification**: route-smoke 131/131 pass. `alembic heads` → `0036_force_rls_tenant_context (head)`. SQLite baseline maintained.

**RISK-CI-PG-02 status**: still open (advisory), materially reduced. Arc-3 closure.

## Recent Work (2026-05-25, later8) — Guarded `g1a2b3c4d5e6_audit_lab_integrity` for ORM-only tables (RISK-CI-PG-02 chain drain)

One commit this arc. Reframe note: investigation that started as the "auth_audit_logs alembic repair" arc (flagged in `later6`) revealed the `3450c02f9c01` migration is structurally correct — both sides of the FK were integer at the chain point where it runs, and `4dfe7c45fffe` correctly handles the integer→UUID conversion of both `users.id` and `auth_audit_logs.user_id` (lines 51, 78-81, 94-96). The actual alembic-in-isolation blocker that surfaces in that chain region is `g1a2b3c4d5e6_audit_lab_integrity.py` referencing four ORM-only tables (`audit_transactions`, `audit_findings`, `audit_reports`, `market_snapshots`).

- Rewrote the migration's upgrade/downgrade bodies into four `DO $$ ... IF EXISTS (SELECT 1 FROM pg_class WHERE relname='...') THEN ... END IF; END $$;` blocks. Pattern matches `0036_force_rls_tenant_context.py` and the prior guard sweep in `24dfb84` + `0cba136`. SQL-layer guard (not Python try/except) is required because PG aborts the transaction on the first failed `op.execute` and ignores every subsequent statement with `current transaction is aborted, commands ignored`.
- Baseline maintained: full SQLite suite 5514 passed / 160 skipped / 0 failed. `alembic heads` reports the canonical `0036_force_rls_tenant_context (head)`.
- Documented in CHANGELOG (later8) a correction of the inaccurate (later6) claim that the auth_audit_logs migration was broken — verify migration bodies against the chain pre-state at that revision, not against the model's end-state types.

**RISK-CI-PG-02 status**: still open (advisory). The architectural fix in `17a1cc0` (workflow refactor) remains the durable solution; this guard is one more brick on the alembic-in-isolation path. Promotion to hard gate still pending N consecutive green CI runs once billing returns.

**End-to-end probe verification (fresh PG)**: against `pg-ci-probe2` (port 5499), schema dropped and `alembic upgrade head` rerun. Chain advanced past `g1a2b3c4d5e6` (guard verified working — audit_* tables cleanly skipped). Next crash at `h1a2b3c4d5e6_company_sso_billing_fields.py`: `relation "companies" does not exist`. `companies` is the next ORM-only table in the crash sequence; 12 migrations in the chain reference it. Per (0cba136) explicit guidance, diminishing-returns reached on per-migration guards. The `17a1cc0` workflow refactor already tolerates this crash class via `set +e` + `_ensure_tables()` finalization. Promoting `backend-postgres` to hard gate now requires either a single "stub-ORM-tables" pre-migration (~1-day write) or ~10 more guard commits — both out of scope this arc.

**Probe state**: restored via `_ensure_tables()` + `alembic stamp head`. At head (`0036_force_rls_tenant_context`).

**Repo state**: master at `d3c46ed`, working tree clean. SQLite baseline 5514 / 160 / 0 unchanged.

## Recent Work (2026-05-25, later7) — PG-suite Drain Arc: NullPool, UPSERT bootstrap, Py3.12 Enum, CORS env-isolation

Continuation of the RISK-CI-PG-02 followup drain (the 83-fail count from `later5`'s end-to-end verification). Five commits this session; pushed to `origin/master` at `466eb43`. Headline: drove the full PG suite from 79 failed + 5 errors → effectively 0 failures against the contamination pattern. Net engineering surface touched: test infrastructure (conftest), 4 test files, 2 production files (`app/services/api_keys.py`, `app/models/api_key.py`).

- `0411742` — NullPool patch on the global PG engine + UPSERT demo/synthetic users in `_pg_seed_session_bootstrap`. Closes the pytest-asyncio cross-loop pollution that caused intermittent `Event loop is closed` + 500s on `/me`/`/dashboard` under shared-state PG runs. NullPool patch must precede `app.main` import or routes capture the old engine.
- `fc99517` — synthetic JWT user now bound to a real company row (UUID `11111111-1111-...`); fixes `'None'`-as-string asyncpg UUID binding when `company_id=None`. Stale `/health` → `/api/health` corrected.
- `75eb4b5` — **real production bug**: Python 3.12 changed `str(Enum)` semantics on str-mixin enums. `str(ApiKeyStatus.ACTIVE)` now returns `"ApiKeyStatus.ACTIVE"`, not `"active"`. Both `verify_api_key_header` and `is_active` evaluated to inactive for every active key. Fix uses `.value`. Also: `datetime.utcnow()` → `datetime.now(UTC)` (naive→TIMESTAMPTZ produced 4-hour timestamp drift on Windows for the expired-key test). Path fix `/admin/api-keys` → `/api/admin/api-keys`.
- `18b1b36` — scoped fixture cleanup: `test_auth.py::cleanup_db` no longer wholesale-DELETEs users (FK-violates against e2e leftovers); `test_policy_service_fix.py` gained the missing `demo_user` fixture; `test_workflow_full.py` expanded the expected-error filter to accept `IntegrityError`/FK on negative paths.
- `466eb43` — `_fresh_settings_class()` helper in `test_auth_cookies.py::TestCorsConfig`. The 4 final failures were env-var bleed: earlier suites set `os.environ` at module-import time without cleanup, and `importlib.reload(cfg_module)` re-reads env, so contamination leaked into the reloaded `Settings()`. Helper snapshots/pops `CORS_*` keys around the reload then restores. Verified locally against simulated `CORS_ALLOW_ORIGINS='*' + CORS_ALLOW_CREDENTIALS='false'` contamination.

**Gotchas worth carrying forward**:
1. Python 3.12 `str(Enum)` on `str`-mixin enums returns `"Class.MEMBER"`, not the value. Always `.value` for comparisons; `StrEnum` if defining new ones.
2. pytest-asyncio per-function loops + SQLAlchemy `QueuePool` = cross-loop asyncpg corruption. Use `NullPool` for test engines.
3. `ASGITransport` doesn't fire FastAPI lifespan → seed functions referenced from `lifespan` never run for `httpx.AsyncClient`-based tests. Seed in conftest session-scope explicitly.
4. `importlib.reload()` re-reads `os.environ`; test-suite env mutations bleed through reload boundaries. Snapshot/pop keys around the reload to isolate.
5. asyncpg + TIMESTAMPTZ + naive `datetime` = wrong stored timestamps (treated as local time). Always `datetime.now(UTC)`.

**RISK-CI-PG-02 status**: Followup drain advancing. Hard-gate promotion still gated on N consecutive green runs once CI billing returns.

**Repo state**: master at `466eb43`, working tree clean, in sync with origin. Backend SQLite baseline unchanged at 5514 passed / 160 skipped / 0 failed.

## Recent Work (2026-05-25, latest) — RISK-CI-PG-02 Architectural Fix + Doc Accuracy Pass

Today's arc closed RISK-CI-PG-02's primary mitigation by realigning the CI workflow with the production bootstrap sequence — the durable fix the prior migration-guard work pointed at. Three commits landed.

- `17a1cc0` — `ci(backend-postgres): mirror production bootstrap sequence (RISK-CI-PG-02)`. Replaces the single brittle `alembic upgrade head` step in `.github/workflows/ci.yml::backend-postgres` with the three-step production-mirror sequence: `alembic upgrade head` (non-fatal, `set +e/-e` bracketed) → `python -c "from app.main import _ensure_tables; asyncio.run(...)"` → `alembic stamp head`. This eliminates the root cause that the advisory job kept revealing — alembic-in-isolation crashes the moment any migration `ALTER TABLE`s an ORM-only table (`positions`, `execution_proposals`, `policy_instances`, `audit_*`). The 8 migration guards from `24dfb84` + `0cba136` remain as belt-and-suspenders. Local import verification passed; SQLite suite still 5514/160/0.
- `72d5add` — `docs(claude): correct stale "known gap" note — both RLS startup guards exist`. CLAUDE.md §9.1 had described the JWT-side guard as a "known gap" since before RLS-02 mitigation B landed; replaced with concrete documentation of both `assert_api_key_routes_safe` and `assert_routes_have_canonical_auth`, the allowlists they enforce, and the discipline expected when extending either one.
- Baseline number in CLAUDE.md §6 brought current (5507 → 5514).

**RISK-CI-PG-02 status**: Open (advisory) → Mitigated (advisory). Followup (a) marked done in `OPEN_RISKS.md`. Promotion of `backend-postgres` to a hard gate is now a single milestone away — needs N consecutive green runs against fresh PG to prove the workflow change stable.

**CI billing**: Still blocked despite user's account upgrade. Both commits show the 4–5s/empty-`steps[]` quota-exhaustion signature documented in CLAUDE.md §9.5 + memory. The CI fix is correctly shipped to origin/master but validation is deferred, not blocked.

## Recent Work (2026-05-25, earlier) — RISK-CI-PG-02 Migration Guard Sweep + Root-Cause Identification

Two commits earlier this session shipped 8 defensive migration guards and identified the architectural root cause that motivated the workflow refactor above.

- `24dfb84` — first pass: `4dfe7c45fffe` (users.id UUID conversion, `DO $$ IF data_type='integer' THEN … END IF; END $$;` wrapper + `USING NULL::uuid` because `integer::uuid` is invalid PG syntax) and `a3f8c1d2e4b5` (Python try/except replaced with `DO $$ IF EXISTS pg_class ... END IF; END $$;` — Python try/except can't reset poisoned PG transactions).
- `0cba136` — second pass: `b7d2e4f1a9c3`, `k1a2b3c4d5e6`, `0036_force_rls_tenant_context`, `f81cffe7f9ee` (dialect-branched: PG raw guards, SQLite preserved via `op.create_index` path), `c9f3a2b1d4e5`, and `f1a2b3c4d5e6` (early-return on missing `policy_instances`).
- After all 8 guards, fresh-PG probe still failed at `audit_transactions` — another ORM-only table. Decision point: keep one-migration-at-a-time guarding (diminishing returns) or fix the architectural divergence between CI and production. The architectural fix is `17a1cc0` above.

## Recent Work (2026-05-24, latest) — RISK-AUTH-RLS-02 Mitigation C: Dashboard Refactor (Root-Cause Elimination)

Followup to mitigation B that finally retires the parallel `_resolve_user` helper. With the startup guard in place, the structural defense was firm — but the allowlist still carried 7 dashboard exceptions tagged "deferred refactor". This arc closes that deferred work.

Commit: `81d0064` — `refactor(dashboard): use Depends(get_current_user); delete _resolve_user (RISK-AUTH-RLS-02 followup)`

- `backend/app/api/routes/dashboard.py` — all 7 endpoints (`summary`, `recent-runs`, `pending-approvals`, `team-activity`, `branch-comparison`, `pipeline-status`, `aggregate`) now take `user: User = Depends(get_current_user)`. `_extract_bearer` and `_resolve_user` deleted entirely (~25 LOC removed).
- `backend/app/core/dependencies.py` — `NO_AUTH_ROUTE_ALLOWLIST` shrunk from 42 → 35 entries (the 7 dashboard paths removed).
- `backend/tests/test_dashboard_rls_injection.py` — rewritten to call `get_current_user` directly (the 3 existing scenarios) + new structural test asserting every dashboard route has `Depends(get_current_user)` in its dependant tree.
- `backend/tests/test_dashboard_routes.py` — mock `side_effect` sequences updated for the 2 extra `set_config` execute slots `inject_tenant_rls` consumes; 3 obsolete `_extract_bearer` tests deleted.
- `backend/tests/test_canonical_auth_startup_guard.py` — canonical-paths test now pins `/api/hedge/run` instead of `/api/v1/dashboard/summary`.

**Verification**: 5514 passed / 160 skipped / 0 failed on SQLite (was 5507; net +7). 56/56 across the 4 affected test files. Production app boots clean against its own canonical-auth guard.

**Status**: Pushed to `origin/master` at `81d0064`. RLS-02 now mitigated three ways — runtime fix (A), structural startup guard (B), root cause eliminated (C). No deferred items remain on this risk.

## Recent Work (2026-05-24, later) — RISK-AUTH-RLS-02 Mitigation B: Canonical-Auth Startup Guard

Hardening follow-up to the RLS-02 fix. The dashboard `_resolve_user` patch closed the active bug, but the underlying structural gap (a route quietly skipping the canonical `get_current_user` dep) had no automated check. The RLS-01 guard catches only the API-key direction. Without a complementary check, the next parallel auth helper would silently empty RLS-forced queries again.

Commit: `4607acc` — `feat(auth): startup guard for canonical auth deps (RISK-AUTH-RLS-02 mitigation)`

- `backend/app/core/dependencies.py` — `NO_AUTH_ROUTE_ALLOWLIST` (42 routes, categorized: root/docs, health, auth issuance, signature-auth webhooks, public market data, seed/demo, the stateless engine endpoint, and the 7 dashboard routes pending refactor) + `assert_routes_have_canonical_auth(app, allowlist)` walking every APIRoute's dependant tree.
- `backend/app/main.py` — guard wired into `lifespan` after the existing RLS-01 guard; `RuntimeError` from the guard propagates and blocks deployment, other exceptions are non-fatal warnings to avoid bricking startup on an unrelated import issue.
- `backend/tests/test_canonical_auth_startup_guard.py` — 9 tests including `test_real_main_app_passes_guard` (regression boot of the production app against its own guard with the shipped allowlist).

**Verification**: 19/19 tests pass across the three guard files (`test_canonical_auth_startup_guard` 9 + `test_api_key_rls_startup_guard` 7 + `test_dashboard_rls_injection` 3). Production app boots clean against its own guard.

**Status**: Pushed to `origin/master` at `4607acc`. CI billing block continues — local validation only. `OPEN_RISKS.md` and `CHANGELOG_AI.md` updated.

## Recent Work (2026-05-24, late) — RISK-AUTH-RLS-02: Dashboard JWT Path RLS Injection

Continuation of the RLS hardening arc that shipped RISK-AUTH-RLS-01 mitigation (option 3 startup guard) earlier in the day. While investigating the broader RLS surface beyond the API-key path, discovered that `app/api/routes/dashboard.py::_resolve_user` decodes JWTs directly without calling `set_tenant_rls_context()` — a parallel auth helper that predates `core/dependencies.py::get_current_user`. With migration 0036 forcing RLS on `positions` and `calculation_runs`, the policy `COALESCE(NULLIF(...,''), '00000000-...')` treats the unset contextvar as the NO_TENANT sentinel, so every dashboard aggregate query (summary, recent-runs, etc.) against those RLS-forced tables silently returned empty.

Distinct from RISK-AUTH-RLS-01: that risk covered the *API-key* path (latent — only diagnostic routes used it). RLS-02 was *active* on JWT users in production.

Commit: `27696c8` — `fix(rls): inject tenant context in dashboard JWT path — RISK-AUTH-RLS-02`
- `app/api/routes/dashboard.py` — `_resolve_user` now calls `set_tenant_rls_context(tenant_id, bypass=is_superuser)` after the User lookup. Relies on `TenantRLSAsyncSession.execute()` to auto-inject on the next query (marker change). Explicit `inject_tenant_rls` deliberately omitted because it consumes mocked execute slots in existing dashboard route tests.
- `backend/tests/test_dashboard_rls_injection.py` — 3 new regression tests: contextvar set to company_id, superuser bypass flag, contextvar stays cleared on 401-rejected path.
- `.claude/state/OPEN_RISKS.md` — RISK-AUTH-RLS-02 entry, opened+closed same day.

**Verification**:
- Backend full suite: **5507 passed / 160 skipped / 0 failed** on SQLite (was 5504 — +3 new tests, 0 regressions)
- Dashboard route tests: 49/49 (test_dashboard_routes + test_dashboard_rls_injection + test_api_key_rls_startup_guard)
- CI: run `26376164714` still 3s instant-fail across all jobs — billing block continues; commit landed on origin/master at `27696c8`

**Followups**: A complementary startup guard for "routes that read positions/calculation_runs but don't depend on `get_current_user`" would be the natural next defense layer. Consider hoisting `_resolve_user` into `core/dependencies.py` or replacing it with `Depends(get_current_user)` to eliminate the parallel auth helper.

## Recent Work (2026-05-24) — CI Repair Arc Continuation: Migration Fix + Smoke Job Wiring

Continuation of the 2026-05-23 CI repair arc. Master CI ran green once on `c1f153e` (run 26350186757), exposing RISK-CI-PG-02 (audit_logs DuplicateTable) as the next advisory failure. Six commits this arc:

1. `137372b` — `CURRENT_STATE.md` rollup for the 2026-05-23 arc
2. `830f4ee` — `e2180e1dd4e7` migration: `DROP TABLE IF EXISTS audit_logs / auth_audit_logs / refresh_tokens CASCADE` before the create_table calls (legacy chain already created them; rebuild had been a DuplicateTable on fresh PG)
3. `15fd8fe` — same migration: changed three user_id columns from `sa.UUID()` to `sa.Integer()` to match users.id at that chain point (next migration `4dfe7c45fffe` converts the graph to UUID in lock-step; creating UUID FKs against an INTEGER PK fails)
4. `0943701` — same migration: switched `op.add_column` for is_superuser/token_version to raw `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` (those columns are created earlier by `a1ed712e8018` and `3e9f47487b7f` respectively — DuplicateColumn on fresh chain)
5. `fe025cf` — same migration: replaced `with op.batch_alter_table(...) as batch_op: batch_op.drop_column(...)` with raw `ALTER TABLE users DROP COLUMN IF EXISTS` per column. `batch_alter_table` defers DDL until the `with` block exits, so the try/except inside the block could never catch the UndefinedColumn raised at flush
6. `008f830` — `frontend/playwright.config.ts`: added `smoke` Playwright project (`testMatch: /e2e[\\/]smoke[\\/].*\.spec\.ts/`, resolves to 44 tests: 43 nav-smoke + 1 full-journey)
7. `97636e9` — `.github/workflows/ci.yml`: added advisory `e2e-smoke` job (10-min step timeout, `continue-on-error: true`) running `--project=smoke`
8. `d173aea` — `OPEN_RISKS.md`: marked RISK-CI-E2E-01 followup #2 partially done (smoke split shipped; CI-local backend still open)

**Verification (local-only; CI runners billing-blocked partway through arc)**:
- `alembic heads` → `0036_force_rls_tenant_context` (single head, chain intact)
- `pytest tests/test_routes_smoke.py` → 131/131 passed
- Full backend pytest suite → **5497 passed / 160 skipped / 0 failed (3m58s)** on SQLite, no regressions
- `npx playwright test --project=smoke --list` → 44 tests resolved correctly
- `e2e-smoke` job appears in workflow run job list (wiring valid; needs runners to actually execute)
- Live production verified: `hedgecore.onrender.com/api/health` 200/db:ok; `ordr-terminal.vercel.app` 200; API key gate enforcing 401

**Pending CI verification** (all commits landed on origin/master, waiting on billing):
- Migration fix needs `backend-postgres` advisory job (currently 2s instant-fail with BlobNotFound — billing block, not a real failure)
- Smoke job needs 10-min stability over N consecutive green runs before promotion candidacy

Migration fix is CI-only — production already has `e2180e1dd4e7` applied (the revision is from the original chain, not new); my edits only matter for fresh-DB `alembic upgrade head` scenarios. Render auto-deployed `d173aea` and prod stayed healthy.

## Recent Work (2026-05-23) — CI Repair Arc: Master Green for First Time Since 2026-05-13

GitHub Actions billing was restored mid-session ("I fixed github, you continue"). Master CI had been red for 10+ days. Worked through 8 commits to clear every hard-gate failure exposed once jobs could actually run:

1. `69804bf` — `backend/migrations/env.py` reads `DATABASE_URL` directly (was falling through to compose hostname `hedgecalc_db`)
2. `52e7e3a` — regenerated `frontend/package-lock.json` (736 inserts) so `npm ci` could resolve
3. `b1aeaba` — added `aiosqlite==0.20.0` to requirements (missing test driver)
4. `54c3559` — fixed `frontend/e2e/accounting/*.spec.ts` helper imports (`../../helpers/auth` → `../helpers/auth`); two specs had been short-circuiting the entire Playwright suite for 10+ days
5. `f1a9743` — pinned wall-clock-sensitive token-bucket assertions in `test_sprint2_resilience.py` + `test_cycle_lifecycle.py`
6. `732fe8e` — demoted E2E job to `continue-on-error: true` (RISK-CI-E2E-01: 237-test suite cannot finish in runner window)
7. `8ea2bfe` — Docker build context fix: `docker build -f backend/Dockerfile -t ordr-backend:ci .` (was `./backend`, which broke `COPY backend/...`)
8. `88cd86d` — cleared 14 OS CVEs + 6 Python CVEs flagged by Trivy: base image `bullseye → bookworm`, `apt-get upgrade`, bumped fastapi 0.118.0→0.121.0 / starlette 0.48→0.49.1 / cryptography / PyJWT / python-multipart / Mako
9. `c1f153e` — moved E2E `timeout-minutes: 20` from job-level to step-level; job-level timeout converts to "cancelled" which poisons workflow result regardless of `continue-on-error`

Run 26350186757 (commit `c1f153e`): `conclusion=success`. All 5 hard-gates green (Backend, Frontend, Architecture Governance, Secret scan, Docker build). Advisory jobs: backend-postgres failing on RISK-CI-PG-02 (audit_logs DuplicateTable in alembic chain), E2E passed within window (variance, not certification).

Two new risks opened: RISK-CI-E2E-01 (E2E suite has not actually run end-to-end on master in 10+ days — promotion back to hard gate is a launch-readiness milestone), RISK-CI-PG-02 (alembic `audit_logs` already exists — bisect required, advisory job blocked).

## Current Sprint
**Launch Readiness — Production Hardening** — COMPLETE (2026-04-23)
- Track 1 — connector framework + 5 providers + hub UI + ADR-0015: ✅ (commit `7b616b1`)
- Track 2.2 — 20 per-feature error boundaries + Sentry feature-tagged scope: ✅ (commit `5604cb1`)
- Track 2.3 — any-type sweep, new `extractErrorDetail` helper, typed useState: ✅ (commit `c331c90`)
- Track 3 — nav-smoke + 14 treasury-suite specs + connector hub stubs: ✅ (commit `33b5cd7`)
- Track 4 — hash-chain cron, k6 baseline doc, Vercel preview CORS, prod encryption validator, structured error handlers: ✅ (commit `c331c90`)
- Track 5 — work items #22/#23 closed as superseded; #19/#20/#24 remain blocked on user/ops credentials

## Recent Work (2026-05-17) — fbc1eb1 follow-up audit

Continued the post-incident audit of `fbc1eb1` for other PG-only landmines beyond the SET LOCAL bug:

- **Migration 0036**: `FORCE ROW LEVEL SECURITY` is on `positions` and `calculation_runs`. Policies use `COALESCE(NULLIF(current_setting('app.current_tenant_id', true), ''), '_NO_TENANT')` — missing tenant context silently filters every row out (no error, just empty results). Catch is only via integration testing against PG.
- **JWT auth path**: `core/dependencies.py::get_current_user` (lines 129–132) correctly injects tenant + bypass for superusers. Verified.
- **API-key auth path**: `deps/api_key_auth.py::get_api_key_principal` and `middleware/api_key_auth.py` validate the key but **never inject tenant context**. Today this is **latent** — `get_api_key_principal` is only consumed by `/system/whoami/api-key` and `/system/db-tables`, neither of which reads RLS-protected tables. It becomes a P1 the moment an API-key dep is added to any business endpoint. Tracked as RISK-AUTH-RLS-01.
- **Governance middleware** (`app/middleware/governance.py`): Imports `synex_kernel` from local repo path; fail-open via `_kernel_unavailable` flag if import fails. Wired in `main.py` at the expected index per `test_middleware_order.py` canonical order. Production import path is fine (synex_kernel is in the repo).
- **Argon2id+pepper API-key verification**: `verify_api_key_header` reads `api_keys` (not RLS-protected), updates `last_used_at` (now wrapped in try/except). No new landmine.
- **Voice agent**: Fail-closed on missing market rate (no more 17.24 fallback). Safer.

No additional production fix shipped. Findings recorded; `RISK-CI-PG-01` re-classed to "Mitigated (advisory)" given `137c8a2` shipped the PG-in-CI job already.

## Recent Work (2026-05-16, 17:24Z) — P1 Incident: RLS injection broken on asyncpg

`/api/health` was returning 503 in prod ("checks.db" = `asyncpg.exception…`). Diagnosed within minutes of the post-deploy smoke check after the launch-readiness commits landed: `TenantRLSAsyncSession.execute()` was issuing `SET LOCAL app.current_tenant_id = :tenant_id`, and PostgreSQL's grammar rejects bind parameters inside `SET` statements. asyncpg's extended query protocol surfaced this as `PostgresSyntaxError` on every DB query — meaning every authenticated DB-touching endpoint had been failing since `fbc1eb1` shipped on 2026-05-13.

Root cause: the unit test (`test_inject_tenant_rls_executes_set_local`) used a mocked AsyncMock session, and the PG-only tests inlined UUIDs via f-string instead of bind params. CI runs against SQLite, so the production injection path was never exercised against a real driver.

Forward fix: `151c591` — replaced `SET LOCAL <var> = :param` with `SELECT set_config('<var>', :param, true)`. Same transaction-local semantics, but `set_config()` is a function call so the bind protocol works. Health restored 17:28Z (~4 min after push).

Post-mortem: `docs/incidents/2026-05-16-rls-set-local-bind-params.md`. Two new HIGH risks opened (RISK-CI-PG-01: no PG tests in CI; RISK-OPS-MON-01: no 5xx alert, no auto-rollback — both directly responsible for the 3-day silent degradation).

## Recent Work (2026-05-16) — State-Drift Reconciliation

Historian re-anchored after a ~19-day silent window. In-depth audit of 15 commits that landed on master between 2026-04-28 and 2026-05-13 (substantive: `fbc1eb1` enterprise audit hardening — bcrypt→Argon2id+pepper, FORCE RLS, `synex_kernel/` namespace; `3f8d747` POST /v1/seed/demo-reset; `d876e7c` POST /v1/positions/bulk; nine E2E spec rewrites; `0d34942` k6 JWT Bearer rewrite; engine_v1 mypy --strict fixes).

Today's outputs: ADR-0020 (retroactive canonization of `fbc1eb1`), coverage CI gate raised 60→70 in `.github/workflows/ci.yml`, `work_items.blocked_reason` column added with items #19/#20/#24 reclassified `blocked` with explicit reasons, risk register cleaned (R-003 closed, R-005 archived, R-007 closed), 4 new runbooks under `docs/runbooks/`, new `backend/tests/test_routes_smoke.py`. Not re-verified in-session: exact post-cleanup row counts in `open_risks` and pytest pass against the new 70% gate.

## Recent Work (2026-04-27) — Sub-project A: Live ERP End-to-End COMPLETE

Three wire-up bugs fixed:
- QBO + Xero `exchange_code()` now writes `company.settings["erp_system"]` after OAuth (was always "CSV")
- GL posting route now calls `connector.post_journal(tenant_id, payload)` (handles token refresh internally) instead of legacy `erp_credentials` path
- OAuth callback redirects to `/accounting-oauth-callback?system={provider}` (was `/settings/connectors` — non-existent route)

New features shipped (commits `5a95c5f` → `1f177ef`):
- `POST /v1/connectors/{provider}/test-post` — synthetic balanced entry, no WORM row, `trades.create` permission gate
- GL Postings page: "Post to QB" / "Post to Xero" / "Export CSV" button derived from connector status; `posted_ref` badge (QBO deep-link); Retry button on failure
- Accounting Connection page: real OAuth popup calls backend `/authorize`; HTTPS scheme validation before `window.open`; Test Connection button in connected card

Security/quality improvements:
- APPROVED status guard added to ERP posting path
- `payload.assert_balanced()` enforced before hitting ERP API
- URL-encoded error messages in OAuth redirect URLs
- `API_KEY_AUTH_DISABLED` env bypass for test isolation
- `ResponseValidationError` handler added to `main.py`

Tests: +16 (4 `test_gl_post_wire`, 4 `test_connector_test_post`, 2 `test_oauth_redirect`, 6 connector fix assertions)
Baseline: **5495 passed, 0 failed, 158 skipped (PG-only)**; `tsc --noEmit` clean; `next build` successful

## Previous Work (2026-04-23) — Live ERP / Accounting Connector Framework COMPLETE

- Foundation (~1,100 LOC, 7 modules under `backend/app/connectors/`): `base.py`, `errors.py`, `token_vault.py` (Fernet MultiFernet), `oauth_state.py` (HS256+Redis replay guard), `rate_limiter.py` (Redis Lua token bucket), `retry.py` (exp backoff + circuit breaker), `registry.py`
- Five provider packages (~1,800 LOC): `quickbooks/`, `xero/`, `netsuite/`, `sage_intacct/`, `dynamics365/`
- 11 new unified routes in `v1_connectors.py`; `_connector_error_to_http` via `exc.http_status`
- Schemas: `ProviderMeta`, `ConnectorAuthorizeRequest/Response`, `ConnectorStatusResponse`, `ConnectorHealthResponse`, `COAResponse`, `JournalPostRequest/Response`, `ConnectorConnectFormRequest`
- Config: `CONNECTOR_ENCRYPTION_KEY` + per-provider `CLIENT_ID/SECRET/REDIRECT_URI/WEBHOOK_KEY`; production validator refuses boot if creds populated without encryption key
- Frontend: `connectorClient.ts` + connector hub page (~450 LOC) with StatusDot, OAuth redirect, Intacct FormModal
- ADR-0015 accepted (`docs/architecture/adr/0015-live-erp-connector-framework.md`)
- Validation: `tsc --noEmit` clean; backend import smoke pass; engine_v1 untouched

## Previous Work (2026-04-21) — Mobile-Responsive Core Pages COMPLETE

- Foundation: Viewport meta in `layout.tsx`, responsive breakpoints (`--bp-sm/md/lg`) in `globals.css`, safe-area insets, 44px touch targets
- Sidebar: Mobile hamburger header, fixed overlay sidebar with backdrop, auto-close on nav selection
- Dashboard: Mission cards 3→1 column, chart+FX rates vertical stack, macro indicators 5→2 column, operations grids 2→1 column
- Calculate: Step grids stack on mobile, bucket table horizontal scroll
- Payments: 4→2 column KPIs, 3→1 column forms, 2→1 column grids
- Portfolio: 6→3 column risk cards, chart+table vertical stack
- Cash Positions: Tables wrapped in `overflowX: auto` with `minWidth`
- Hook: `useBreakpoint.ts` with `useIsMobile` and `useIsSmallMobile`
- Validation: tsc --noEmit clean, next build --no-lint pass (117 pages)

## Previous Work (2026-04-18) — P0-C: Counterparty Scoring Hub COMPLETE

- Models: `Counterparty` + `CreditLimit` with 6 indexes + unique(tenant_id, name); counterparty has cached exposure metrics (last_exposure_usd, last_pfe_usd, risk_level_cached, last_scored_at)
- Migrations: `0029_counterparty_tables`, `0030_counterparty_permissions` (counterparty.read → all roles; counterparty.write → admin/treasurer/risk_analyst)
- Schemas: 9 Pydantic v2 classes in `schemas_v1/counterparty.py` (CRUD + Exposure + PortfolioRisk + LimitBreach)
- Service: `counterparty_service.py` — CRUD counterparty + CRUD credit limits (single-active-per-type invariant) + compute_exposure (wraps engine_v1.counterparty_risk, detects breaches at ≥80%/100%) + compute_portfolio_risk; reused P0-B WORM audit pattern with hash-chain FOR UPDATE
- Routes: `v1_counterparty.py` — 9 endpoints under `/v1/counterparties` (CRUD, limits CRUD, exposure, portfolio-risk); typed error mapping (404/409/422); Professional tier + `counterparty.read|write` RBAC
- Frontend: `counterpartyClient.ts` with `CounterpartyApiError`; `/counterparties` hub list+create; `/counterparties/[id]` detail with metadata + limits CRUD + ad-hoc exposure compute showing breach flags; sidebar entry under DEBT & IR RISK
- Architectural note: `compute_exposure` accepts caller-supplied positions (Position ORM has no counterparty_id in v1); engine stays pure
- tsc clean; `next build` 0 errors; 9 routes registered

## Previous Work (2026-04-18) — P0-B: Pre-Trade TCA COMPLETE

- Model: `TransactionCostEstimate` (15 cols, WORM-participating, FK to settlement_events)
- Migrations: `0027` (table) + `0028` (tca.read, tca.estimate RBAC)
- Schemas: 7 Pydantic v2 classes in `schemas_v1/tca.py`
- Service: `tca_service` — estimate_pre_trade, attach_to_calc_run (idempotent + eager), reconcile_actual (SoD asymmetric: post_calc blocks self / pre_trade allows self), auto_reconcile_on_settlement (non-fatal), get_accuracy_report; atomic commit pattern; `FOR UPDATE` hash-chain lock; tenant guards on all queries
- Routes: `v1_tca.py` — 6 endpoints; non-fatal integration in `v1_calculate.py`
- Frontend: `tcaClient.ts` with `TCAApiError` typed class; `/pre-trade-tca` estimator page; `/pre-trade-tca/accuracy` dashboard (dynamic Q-YYYY); `TCATab` component on audit-lab run detail; sidebar entry with Calculator icon (Professional tier)
- Review fixes: WORM atomicity, hash-chain race, cross-tenant guards, settlement tx boundary, typed errors
- `tsc` clean; `next build` 117 pages; 5308 tests collect cleanly; browser smoke confirmed

## Previous Work (2026-04-17) — Audit Lab UX Overhaul COMPLETE

- Demo page rebuilt: hero, KPI strip, MarkupByMonthChart, CounterpartyMatrix, findings, trust rail, CTA
- Fixture enriched: `markupByMonth`, `transactions` (11), `findings` (3), `trustSignals` (3), `getDemoCounterpartyStats()`
- Quality fixes: `cpStats` empty guard, `findings[2]` safe lookup, division-by-zero guard, `sevColor` camelCase
- Chunks 2–5 verified pre-implemented: upload UX, hub BETA removal + empty state + run list, run detail (5 KPIs + hash + expandable findings + Verification tab), sidebar Activity Log rename
- Browser confirmed: `/audit-lab/demo` renders all 6 acts correctly (screenshot captured)
- `next build`: clean (exit 0); `tsc --noEmit`: clean

## Recent Work (2026-04-17) — Phase 4: Debt Management + IR Risk COMPLETE

- Engine: `ir_curve_engine`, `swap_valuator`, `swaption_engine` (annuity bug fixed), `debt_cashflow_engine`, `ir_hedge_effectiveness` (IFRS 9)
- Models: `DebtFacility/Drawdown/Covenant` + `IRSwap/VolSnapshot/HedgeRun` (WORM + hash chain)
- Services: `debt_service` (6 ops), `ir_swap_service` (MTM + DV01), `ir_hedge_service` (WORM run + evidence bundle)
- Routes: `v1_debt` (8 endpoints) + `v1_ir_risk` (7 endpoints); `debt.read/write` + `ir_risk.read/write` RBAC
- Migrations: 3 Alembic migrations incl. WORM PG trigger + 4 new RBAC permissions
- Frontend: `debtClient.ts` (12 API fns), `/debt`, `/debt/[id]`, `/ir-risk` pages, sidebar DEBT & IR RISK section
- 28 new tests all pass; `next build` clean; `tsc --noEmit` clean

## Previous Work (2026-04-16) — Phase 3: Intelligence Tier COMPLETE

- `IntelligenceQueryLog` ORM model (9 cols, composite index) + migration q1a2b3c4d5e6
- `intelligence_service.py`: advisory-only — query_intelligence, draft_commentary, get_usage_stats, build_treasury_context; prompt hashes (SHA-256) stored, never raw prompts
- `v1_intelligence.py`: 4 endpoints (POST /query, POST /commentary, GET/PATCH /settings); error map: APIError→502, missing key→503, unsupported→422, tier→402, role→403
- Company model: `intelligence_enabled` opt-in column; plan enforcement: intelligence:3 in PLAN_HIERARCHY
- Config: ANTHROPIC_API_KEY + ANTHROPIC_MODEL; CashAuditEventType: INTELLIGENCE_QUERY (now 29 values)
- ADR-0014: advisory-only contract (no AI writes to WORM tables)
- Frontend: CmdKOverlay (CMD+K global, advisory disclaimer), /intelligence settings+usage page, intelligenceClient.ts (4 functions), INTELLIGENCE sidebar section (Brain icon), AI commentary on hedge-effectiveness run rows
- PlanGate + usePlanGate + usePlanRedirect: intelligence:3 tier rank; PlanTier union extended
- Backend: 5040 passed, 0 failed, 158 skipped (14 intelligence tests all pass)
- tsc --noEmit: CLEAN; next build: PASS

## Previous Work (2026-04-14) — Treasury Suite Phase 2b: Cash Flow Forecasting COMPLETE

- CashForecastItem + CashForecastSnapshot ORM models (`backend/app/models/cash_forecast.py`)
- Migration 0022: cash_forecast tables + indexes + unique constraint
- `forecast_engine.py`: pure-function engine — compute_forecast (13w/12m), expand_recurring_items (6 frequencies), scenario shifts, liquidity gap detection, multi-currency tracking, confidence breakdowns
- `forecast_service.py`: DB orchestrator — get_forecast, items CRUD, run_scenario, get_liquidity_gaps, save_snapshot, get_variance
- `v1_cash_forecast.py`: 10 route endpoints (consolidated, entity, gaps, scenarios, variance, items CRUD, snapshots)
- 10 Pydantic forecast schemas added to `backend/app/schemas_v1/cash.py`
- 2 new CashAuditEventType enum values: FORECAST_CREATED, FORECAST_SCENARIO_RUN
- 19 new tests across 3 files (test_forecast_engine, test_forecast_service, test_v1_cash_forecast_routes)
- Frontend: `/cash-forecast` page (4-tab: FORECAST waterfall, GAPS alerts, VARIANCE table, ITEMS CRUD + scenario panel)
- `cashClient.ts`: 5 interfaces + 8 API functions added for forecast endpoints
- AppSidebar: Cash Forecast nav item (TrendingUp icon, professional tier gate)
- Backend: 4896 passed, 0 failed, 158 skipped (PG-only)

## Previous Work (2026-04-14) — Treasury Suite Phase 2a COMPLETE

- 5 ORM models, migrations 0017-0021, 5 services (legal_entity, bank_account, bank_connection, cash_balance, cash_audit), encryption, 15 schemas, 5 route files (29 endpoints), 7 test files
- Frontend: cashClient.ts (29 functions), 4 pages (/cash-positions, /settings/legal-entities, /settings/bank-accounts, /settings/bank-connections)
- Backend: 4877 passed

## Previous Work (2026-04-14) — Sprint 56-61: Treasury Suite Phase 1 COMPLETE

- JournalEntry + GLAccountMapping models: SHA-256 hash chain, 5-state machine (DRAFT->PENDING_APPROVAL->APPROVED->POSTED/REJECTED), `before_delete` WORM hook
- TreasuryTransaction strict-WORM spine: per-record SHA-256 hash, append-only PostgreSQL trigger
- SettlementEvent WORM model: per-record `event_hash`, `before_delete` hook
- Migrations 0014/0015/0016: journal_entries, gl_account_mappings, treasury_transactions, settlement_events (PostgreSQL WORM triggers)
- gl_service, gl_posting_service, erp_connector_service, settlement_service; v1_gl + v1_erp + v1_settlement routes
- ADR-0009 + ADR-0013; posting adapters QB/Xero/NetSuite/CSV
- Frontend: glClient.ts, /gl-postings, /settlement, /erp-sync, /settings/gl-accounts pages
- BROWSER CONFIRMED (2026-04-14): all 4 pages rendered + sidebar ACCOUNTING group verified
- Backend: 4839 passed, 158 skipped (PG-only), 0 failed. tsc --noEmit CLEAN, next build PASS

## Previous Work (2026-04-13) — Sprint 55: Portfolio Latency Card, Dataset Count Footer, Last-Fail Filter COMPLETE
- OverviewTab: PORTFOLIO ASSESSMENT LATENCY card (avg/median days since last run, UNASSESSED count; color-coded ≤7d/≤30d/>30d)
- RunsTab: DATASETS N KPI in footer (count of distinct dataset_ids in filtered view, hidden ≤1)
- DatasetsTab: LAST FAIL quick filter chip (shows only datasets whose most recent run was ineffective; self-hides if none)

## Previous Work (2026-04-04) — Sprint 6: Regulatory Reporting COMPLETE
- Fixed PageShell-inside-RunsTab bug on hedge-effectiveness/page.tsx
- At-risk hedges monitor to OverviewTab: flags hedges within 10% of 0.80/1.25 IFRS 9 boundary
- Methodology & Standards disclosure panel (EVIDENCE tab): IFRS 9.6.4.1 / ASC 815-20-25 citations
- BROWSER CONFIRMED: IFRS 9 XML + ASC 815 XML buttons, at-risk banner, designation_date header
- 4801 tests passed, tsc clean, next build passed

## Open Risks
- HIGH: Secret rotation pending (S-01) — WORKOS_API_KEY, WORKOS_CLIENT_ID, STRIPE_SECRET_KEY_TEST, STRIPE_WEBHOOK_SECRET not yet in Render env vars
- HIGH: Connector OAuth credentials (CONNECTOR_ENCRYPTION_KEY + {QBO,XERO,NS,INTACCT,D365}_CLIENT_ID/SECRET) not yet provisioned for any deployment — live routes return 424 NotConfigured until env populated
- HIGH: IBKR market data (R-002) — awaiting TWS
- HIGH: Secrets in git history (R-001) — scrub script ready, not yet run
- HIGH: k6 load test baseline incomplete — full staging run (100 VUs, 5 min) not executed
- MEDIUM: Cold starts (RISK-INF-01) — keepalive cron added, Blueprint Sync on Render still pending
- MEDIUM: Render Blueprint Sync — render.yaml changes not yet applied via dashboard
- MEDIUM: NetSuite + D365 change detection — no HTTPS webhooks; scheduled polling deferred (out of v1 scope)
- MEDIUM: Per-feature React error boundaries + Sentry tags (Track 2.2) — not implemented
- MEDIUM: TypeScript `any`-type sweep across 38 pages (Track 2.3) — not executed
- MEDIUM: E2E provider specs (5 ERP connectors + 9 missing nav sections) — pending
- MEDIUM: Production readiness closures — deep health endpoint, hash-chain verifier cron, GDPR anonymize job, SECURITY.md, CORS lockdown

## Validation
- Backend tests: **5495 passed, 0 failed, 158 skipped** (full run 2026-04-27 — includes 16 new ERP wire-up tests)
- tsc --noEmit: CLEAN
- next build: PASS (2026-04-27)
- Coverage: 75%+ (gate: 60%)
- CI: gitleaks + Dependabot + Trivy + coverage gate all active
